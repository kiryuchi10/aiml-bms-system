/**
 * Cells Grid page: renders Dashboard/CellsGrid (summary bar + cell grid + detail drawer).
 * Route: /bms/cells (AppShell wraps this).
 */

import CellsGrid from './Dashboard/CellsGrid'

export function CellsGridPage() {
  return <CellsGrid />
}
