/**
 * ML Console: AI Model Training & Prediction Center (AIModelCenter) 실연동형 UI.
 * /bms/ml 라우트에서 동일한 Shell 내 렌더링.
 */

import AIModelCenter from './AIModelCenter'

export function MLConsolePage() {
  return <AIModelCenter />
}
