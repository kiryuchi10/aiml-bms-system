/**
 * ResearchMode: analytics / ML runs (POST /analytics/{id}/run, GET /analytics/{id}/runs).
 */
export function ResearchMode() {
  return (
    <div className="research-mode">
      <h2>Research Mode</h2>
      <p>Analytics and ML pipelines.</p>
    </div>
  )
}
