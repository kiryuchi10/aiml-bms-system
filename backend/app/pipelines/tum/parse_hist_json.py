"""
Parse track_*.json, charging_*.json (value_description: name_en, variable_name, unit, min_val, max_val).
"""
from app.core.logging import get_logger

logger = get_logger(__name__)


def load_track_json(path: str) -> int:
    """Placeholder: load track JSON."""
    return 0


def load_charging_json(path: str) -> int:
    """Placeholder: load charging JSON."""
    return 0
